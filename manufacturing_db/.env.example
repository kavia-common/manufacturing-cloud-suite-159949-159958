# manufacturing_db - .env.example
# Copy this file to .env and adjust values for your local/dev environment.

# Core connection values
POSTGRES_USER=appuser
POSTGRES_PASSWORD=dbuser123
POSTGRES_DB=myapp
POSTGRES_HOST=localhost

# Default Postgres port is 5432. If you use the included startup.sh it defaults to 5000.
POSTGRES_PORT=5432

# Optional: full URL. If set, overrides the individual POSTGRES_* values.
# Example for default port:
# POSTGRES_URL=postgresql://appuser:dbuser123@localhost:5432/myapp
# Example if using startup.sh defaults:
# POSTGRES_URL=postgresql://appuser:dbuser123@localhost:5000/myapp
